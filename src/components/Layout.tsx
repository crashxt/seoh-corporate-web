import { useEffect,useState } from 'react';
import { ChevronDown,Menu,X } from 'lucide-react';
import { Link,NavLink,Outlet,useLocation } from 'react-router-dom';
const nav=[['Empresa','/empresa'],['Servicios','/servicios'],['Soluciones','/soluciones'],['Soporte','/soporte'],['Contacto','/contacto']];
export default function Layout(){const[open,setOpen]=useState(false);const location=useLocation();useEffect(()=>{setOpen(false);window.scrollTo(0,0)},[location.pathname]);useEffect(()=>{document.body.classList.toggle('menu-open',open);return()=>document.body.classList.remove('menu-open')},[open]);return <div className="site"><header className="topbar"><Link className="brand" to="/" aria-label="SEOH DESIGN TECH"><img src="/logo-seoh-header-2x.png" alt="SEOH DESIGN TECH"/></Link><nav className="desktop-nav" aria-label="Navegación principal">{nav.slice(0,2).map(([l,t])=><NavLink key={t} to={t}>{l}</NavLink>)}<div className="nav-dropdown"><NavLink className="dropdown-trigger" to="/productos">Productos <ChevronDown size={14}/></NavLink><div className="dropdown-menu"><Link to="/productos">Ver catálogo</Link><Link to="/productos?categoria=Seguridad">Seguridad</Link><Link to="/productos?categoria=Automatización">Automatización</Link><Link to="/productos?categoria=Infraestructura">Infraestructura</Link></div></div>{nav.slice(2).map(([l,t])=><NavLink key={t} to={t}>{l}</NavLink>)}<Link className="nav-cta" to="/soluciones">Acceso a apps</Link></nav><div className="mobile-header-actions"><Link className="mobile-apps-button" to="/soluciones">Apps</Link><button className="mobile-menu-button" type="button" onClick={()=>setOpen(!open)} aria-expanded={open} aria-label={open?'Cerrar menú':'Abrir menú'}>{open?<X/>:<Menu/>}</button></div><nav className={`mobile-menu ${open?'open':''}`} aria-label="Navegación móvil">{nav.slice(0,2).map(([l,t])=><NavLink key={t} to={t}>{l}</NavLink>)}<NavLink to="/productos">Productos</NavLink>{nav.slice(2).map(([l,t])=><NavLink key={t} to={t}>{l}</NavLink>)}<Link className="mobile-menu-apps" to="/soluciones">Acceso a aplicaciones</Link></nav></header><main><Outlet/></main><footer><img src="/logo-seoh-header-2x.png" alt="SEOH DESIGN TECH"/><p>© {new Date().getFullYear()} SEOH DESIGN TECH S.A. Todos los derechos reservados.</p></footer></div>}
