import { useEffect } from 'react';
export default function Seo({title,description}:{title:string;description:string}){useEffect(()=>{document.title=`${title} | SEOH DESIGN TECH`;const set=(selector:string,property:string,value:string)=>{let node=document.querySelector<HTMLMetaElement>(selector);if(!node){node=document.createElement('meta');document.head.appendChild(node)}node.setAttribute(property,value)};set('meta[name="description"]','content',description);set('meta[property="og:title"]','content',`${title} | SEOH DESIGN TECH`);set('meta[property="og:description"]','content',description)},[title,description]);return null}
